---
import { getProperties } from "../lib/properties";
import PropertyCard from "../components/PropertyCard.astro";
import SearchFilter from "../components/SearchFilter.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

import "../styles/global.css";

const properties = await getProperties();
let searchQuery = ""; // will bind to search
let filterType = "all"; // will filter property type
---

<BaseLayout
  title="Property Listings in Hyderabad"
  description="Buy apartments, villas, and commercial properties in Hyderabad"
>
<section class="container">
  <h1>Real Estate Listings</h1>

  <!-- Search & Filters -->
  <SearchFilter />

  <!-- Grid -->
  <div class="grid" id="property-grid">
    {properties.map((p) => (
	  <PropertyCard property={p} />
    ))}
  </div>

  <div class="pagination" id="pagination"></div>

</section>
</BaseLayout>
<script type="module" src="/scripts/homepage.js"></script>
